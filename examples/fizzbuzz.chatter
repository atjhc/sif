set x to 1
repeat -- with x = 1 to 100
  if x % 15 is 0 then print line "FizzBuzz" else
    if x % 5 is 0 then print line "Buzz" else
      if x % 3 is 0 then print line "Fizz" else print line x
    end if
  end if
  set x to x + 1
  if x is 100 then exit repeat
end repeat
